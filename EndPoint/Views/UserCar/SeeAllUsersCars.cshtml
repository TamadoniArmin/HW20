@using App.Domain.Core.MoayeneFani.UsersCars.Entities;
@model List<UserCar>
@{
	ViewData["Title"] = "Users";
}
<h2>لیست خودروهای کاربران</h2>
<style>
	body {
		direction: rtl;
		font-family: 'IRANSans', Tahoma, sans-serif;
	}

	table {
		width: 100%;
		border-collapse: collapse;
	}

	th, td {
		border: 1px solid black;
		padding: 8px;
		text-align: right;
	}

	th {
		background-color: #f2f2f2;
	}
</style>
@* <form asp-action="Search" method="post">
	<div class="form-group">
		<label for="Search">Search</label>
		<input type="text" class="form-control" id="Search" name="Partofname" required />
	</div>
	<button type="submit" class="btn btn-primary">Continue</button>
</form> *@
<table class="table">
	<thead>
		<tr>
			<th>Owner Name</th>
			<th>OwnerId</th>
			<th>Car</th>
			<th>CarId</th>
			<th>Plate</th>
			<th>ProductionDate</th>
			<th>CheckedInThisYear</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<th>@item.Owner.FirstName && @item.Owner.LastName</th>
				<th>@item.OwnerId</th>
				<th>@item.Car.Name</th>
				<th>@item.CarId</th>
				<th>@item.Plate</th>
				<th>@item.ProductionDate</th>
				<th>@item.CheckedInThisYear</th>
				@* <td><a class="btn btn-warning" asp-action="EditUser" asp-controller="User" asp-route-Id=@item.Id>Edit</a></td>
				<td><a class="btn btn-danger" asp-action="DeleteUser" asp-controller="User" asp-route-id=@item.Id>Delete</a></td> *@
			</tr>
		}
	</tbody>
</table>
@* <a class="btn btn-primary" asp-action="AddUser" asp-controller="User">Add user</a> *@
@* @if (TempData["LengthOfNC"] != null)
{
	<div class="alert alert-danger">@TempData["LengthOfNC"] </div>
}
@if (TempData["Faile to add"] != null)
{
	<div class="alert alert-danger">@TempData["Faile to add"] </div>
}
@if (TempData["Faile edit"] != null)
{
	<div class="alert alert-danger">@TempData["Faile edit"] </div>
}
@if (TempData["Faile deleting"] != null)
{
	<div class="alert alert-danger">@TempData["Faile deleting"] </div>
} *@

